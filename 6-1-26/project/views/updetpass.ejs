<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Update Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-900 text-white min-h-screen">

  <!-- Navbar -->
  <nav class="bg-zinc-800 px-8 py-4 flex justify-between items-center">
    <h1 class="text-xl font-bold">SecureAuth</h1>
    <a href="/dashboard" class="text-zinc-400 hover:text-white">
      ← Back to Dashboard
    </a>
  </nav>

  <!-- Main -->
  <main class="flex items-center justify-center py-12 px-4">
    <div class="w-full max-w-md bg-zinc-800 rounded-xl p-8 shadow-lg">

      <h2 class="text-2xl font-semibold mb-2">Update Password</h2>
      <p class="text-zinc-400 mb-6">
        For security reasons, choose a strong password.
      </p>

      <!-- Feedback (UI placeholder) -->
      <!-- Success -->
      <!-- <div class="mb-4 p-3 rounded-lg bg-green-600/20 text-green-400 text-sm">
        Password updated successfully
      </div> -->

      <!-- Error -->
      <!-- <div class="mb-4 p-3 rounded-lg bg-red-600/20 text-red-400 text-sm">
        Current password is incorrect
      </div> -->

      <form action="/updetpass"  method="post" class="space-y-4">
 <div>
          <label class="block text-sm mb-1 text-zinc-300">Email</label>
          <input type="Email"
          name="email"
            placeholder="Enter Email"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 focus:ring-2 focus:ring-indigo-500 outline-none">
        </div>
        <div>
          <label class="block text-sm mb-1 text-zinc-300">Current Password</label>
          <input type="password"
          name="password"
            placeholder="Enter current password"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 focus:ring-2 focus:ring-indigo-500 outline-none">
        </div>

        <div>
          <label class="block text-sm mb-1 text-zinc-300">New Password</label>
          <input type="password"
          name="newpass"
            placeholder="Enter new password"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 focus:ring-2 focus:ring-indigo-500 outline-none">
        </div>

        <div>
          <label class="block text-sm mb-1 text-zinc-300">Confirm New Password</label>
          <input type="password"
          name="conpass"
            placeholder="Confirm new password"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 focus:ring-2 focus:ring-indigo-500 outline-none">
        </div>

        <!-- Password Rules -->
        <div class="text-xs text-zinc-400 space-y-1">
          <p>• Minimum 8 characters</p>
          <p>• At least one uppercase letter</p>
          <p>• At least one number</p>
        </div>

        <!-- Actions -->
        <div class="flex gap-3 pt-4">
          <button
            class="flex-1 py-3 bg-indigo-600 rounded-lg hover:bg-indigo-500 transition">
            Update Password
          </button>

          <a href="/dashboard"
            class="flex-1 py-3 border border-zinc-600 rounded-lg text-center hover:bg-zinc-700 transition">
            Cancel
          </a>
        </div>

      </form>

    </div>
  </main>

</body>
</html>
